<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Active__c</fullName>
    <description>If TODAY falls between Start Date and End Date.</description>
    <externalId>false</externalId>
    <formula>IF( AND ( Start_Date__c &lt;= TODAY(), End_Date__c &gt;= TODAY()), true, false)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Today falls between Start Date and End Date.</inlineHelpText>
    <label>Active</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
